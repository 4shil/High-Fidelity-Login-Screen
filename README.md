
  # High-Fidelity Login Screen

  This is a code bundle for High-Fidelity Login Screen. The original project is available at https://www.figma.com/design/FuPCEZafeKbjZpgiqhgrfC/High-Fidelity-Login-Screen.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  